const CONFIG = {
  // Get a free key at https://web3forms.com — messages are emailed to you in production.
  // Leave empty to only save messages locally (view them on admin.html).
  WEB3FORMS_ACCESS_KEY: "",

  // Password for admin.html (change this!)
  ADMIN_PASSWORD: "brew2026",

  MESSAGES_STORAGE_KEY: "brew_bean_messages",
};